# Copy to frontend/.env.production before `npm run build` for the S3 deploy.
# Empty means axios uses the current origin (the CloudFront URL), so /api/*
# is same-origin and CloudFront can forward it to EC2.
VITE_API_URL=
